# v5: prompt-causal plan attention (v4 finding) + EOS-supervised stopping (the ELF
# conditional recipe, band of 64 tail positions) + half the rows train the response against the finished plan.
base_config: train_conditional_common48w_ordered_causal_10k_v1.yml
output_dir: outputs/elf_b_conditional_common48w_ordered_causal_eos_v5
pad_token: eos
eos_tail_loss_tokens: 64
plan_done_frac: 0.5
